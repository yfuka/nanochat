nanochat_base_dir: ${NANOCHAT_BASE_DIR}

stages:
  base:
    out_dir: ${nanochat_base_dir}/base_data
    shard_rows: 20000
    val_rows: 2000
    seed: 42
    inputs:
      - type: hf
        name: kajuma/ABEJA-CC-JA-edu
        config: 10%
        split: train
        text_field: content
        streaming: true
        max_rows: 2000
